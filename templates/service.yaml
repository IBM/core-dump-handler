apiVersion: v1
kind: Service
metadata:
  name: {{ include "ibm-core-dump-handler.fullname" . }}
  labels:
    {{- include "ibm-core-dump-handler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ibm-core-dump-handler.selectorLabels" . | nindent 4 }}
